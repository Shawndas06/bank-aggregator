# 🚀 Инструкция по деплою на бесплатный хостинг

## ✅ Готово к деплою!

Проект настроен для монолитного деплоя - **Backend и Frontend работают вместе** на одном домене.

---

## 📋 Варианты бесплатного хостинга

### 1. Render.com (РЕКОМЕНДУЮ) ⭐

**Преимущества:**
- ✅ Полностью бесплатный тариф
- ✅ PostgreSQL бесплатно
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Простая настройка

**Ограничения:**
- ❌ Redis нужно использовать внешний (Upstash - бесплатный)
- ❌ Сайт "засыпает" после 15 минут бездействия (на бесплатном тарифе)
- ❌ Пробуждение занимает ~30 секунд

**Инструкция:**
1. Зарегистрируйтесь на https://render.com
2. Подключите GitHub репозиторий
3. Выберите "New" → "Blueprint" (если есть `render.yaml`)
   ИЛИ "New" → "Web Service"
4. Выберите репозиторий и укажите:
   - **Runtime**: Docker
   - **Dockerfile Path**: `Dockerfile`
   - **Docker Context**: `.` (корень проекта)
5. Создайте PostgreSQL:
   - "New" → "PostgreSQL"
   - Выберите бесплатный план
   - Скопируйте `DATABASE_URL`
6. Создайте Redis (бесплатно на Upstash):
   - Зайдите на https://upstash.com
   - Создайте бесплатный Redis
   - Скопируйте credentials
7. Настройте переменные окружения в Render:
   - `DATABASE_URL` - из PostgreSQL
   - `REDIS_HOST`, `REDIS_PORT`, `REDIS_PASSWORD` - из Upstash
   - `ALLOWED_ORIGINS` - ваш URL Render (например: `https://your-app.onrender.com`)
   - Остальные из `render.yaml`
8. Нажмите "Create Web Service"
9. Дождитесь деплоя (~5-10 минут)

**После деплоя:**
- Обновите `ALLOWED_ORIGINS` с реальным URL
- Проверьте работу: `https://your-app.onrender.com/health`

---

### 2. Fly.io

**Преимущества:**
- ✅ Бесплатный тариф (3 VM бесплатно)
- ✅ PostgreSQL и Redis бесплатно
- ✅ Быстрый деплой
- ✅ Глобальная CDN

**Инструкция:**
```bash
# Установите Fly CLI
curl -L https://fly.io/install.sh | sh

# Войдите
fly auth login

# Инициализируйте проект
cd bank-aggregator
fly launch

# Создайте PostgreSQL
fly postgres create --name bank-aggregator-db

# Привяжите к приложению
fly postgres attach --app bank-aggregator bank-aggregator-db

# Создайте Redis (через Upstash или встроенный)
# Используйте Upstash для бесплатного Redis

# Задеплойте
fly deploy
```

---

### 3. Другие варианты

- **DigitalOcean App Platform** - бесплатный период 60 дней
- **AWS Elastic Beanstalk** - бесплатный период 12 месяцев
- **Google Cloud Run** - бесплатный тариф (с ограничениями)

---

## 🔧 Настройка для локального запуска

### Через Docker Compose:

```bash
cd bank-aggregator

# Запустить все сервисы
docker-compose up -d

# Логи
docker-compose logs -f app

# Остановить
docker-compose down
```

**Приложение будет доступно:**
- Frontend + Backend: http://localhost
- Swagger Docs: http://localhost/docs
- Health Check: http://localhost/health

---

## 📱 Как сделать скриншоты на телефоне

После деплоя:

1. **Получите URL вашего приложения** (например: `https://your-app.onrender.com`)

2. **Откройте на телефоне:**
   - Откройте браузер на телефоне
   - Перейдите по URL
   - Дождитесь загрузки

3. **Сделайте скриншоты:**
   - iPhone: Нажмите кнопку питания + кнопку громкости одновременно
   - Android: Нажмите кнопку питания + кнопку уменьшения громкости
   - Или используйте встроенную функцию "Скриншот" в браузере

4. **Оптимизация для мобильных:**
   - Приложение уже адаптировано для мобильных
   - Откройте DevTools (F12) → Device Toolbar (Ctrl+Shift+M)
   - Выберите размер экрана телефона
   - Сделайте скриншоты

---

## ⚙️ Настройка переменных окружения

### Для Render.com:

После создания сервиса, перейдите в "Environment" и добавьте:

```env
# Database (из PostgreSQL сервиса Render)
DATABASE_URL=postgresql://user:pass@host:port/dbname

# Redis (из Upstash)
REDIS_HOST=your-redis.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=your-password
REDIS_DB=0

# App
SECRET_KEY=your-secret-key-here
ALLOWED_ORIGINS=https://your-app.onrender.com

# Остальные переменные из docker-compose.yaml
```

---

## 🔍 Проверка работы

1. **Health Check:**
   ```bash
   curl https://your-app.onrender.com/health
   # Должно вернуть: healthy
   ```

2. **API Docs:**
   Откройте: `https://your-app.onrender.com/docs`
   Должна открыться Swagger документация

3. **Frontend:**
   Откройте: `https://your-app.onrender.com`
   Должно открыться приложение

---

## 🐛 Возможные проблемы

### Проблема: Приложение не запускается

**Решение:**
- Проверьте логи: `docker-compose logs app`
- Убедитесь, что PostgreSQL и Redis доступны
- Проверьте переменные окружения

### Проблема: CORS ошибки

**Решение:**
- Обновите `ALLOWED_ORIGINS` с реальным URL
- Убедитесь, что URL начинается с `https://`

### Проблема: База данных не подключается

**Решение:**
- Проверьте `DATABASE_URL`
- Убедитесь, что PostgreSQL доступен из контейнера
- Для Render: используйте внутренний URL PostgreSQL

### Проблема: Redis не работает

**Решение:**
- Используйте Upstash для бесплатного Redis
- Или отключите Redis в коде (если не критично)

---

## 📝 Чек-лист деплоя

- [ ] Зарегистрировались на Render.com
- [ ] Подключили GitHub репозиторий
- [ ] Создали PostgreSQL
- [ ] Создали Redis (Upstash)
- [ ] Создали Web Service
- [ ] Настроили переменные окружения
- [ ] Обновили `ALLOWED_ORIGINS` с реальным URL
- [ ] Проверили работу `/health`
- [ ] Проверили работу `/docs`
- [ ] Проверили работу frontend
- [ ] Сделали скриншоты на телефоне

---

**Удачи с деплоем! 🚀**

После деплоя вы получите URL, который можно открыть на телефоне и сделать скриншоты!

